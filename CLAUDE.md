# Tiny Images Viewer

## Environment

This project uses **pixi** for dependency management (`pixi.toml`). Always run Python via `pixi run python` (not bare `python`/`python3`, which hits the conda base env).

```bash
pixi run python viewer.py
```
